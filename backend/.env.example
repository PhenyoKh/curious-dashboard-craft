
PORT=3001
NODE_ENV=development

# Database
DATABASE_URL=postgresql://username:password@localhost:5432/curious_dashboard
DB_HOST=localhost
DB_PORT=5432
DB_NAME=curious_dashboard
DB_USER=username
DB_PASSWORD=password

# JWT - IMPORTANT: Generate a strong, random secret (at least 32 characters)
# Use: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS
FRONTEND_URL=http://localhost:5173

# Security
# Set to 'production' in production environment
NODE_ENV=development
